declare module '@1hive/1hive-ui' {
  const Main: any
  const GU: any
  const useToast: any
  const useTheme: any
  const Button: any
  const ButtonBase: any
  const IconMenu: any
  const useLayout: () => {
    layoutName: 'small' | 'medium' | 'large' | 'max'
  }
  const Link: any
  const useViewport: () => {
    below: any
  }
  const DataView: any
  const noop: any
  const isAddress: any
  const springs: any
  export {
    Main,
    GU,
    useToast,
    useTheme,
    Button,
    ButtonBase,
    IconMenu,
    useLayout,
    useViewport,
    Link,
    DataView,
    noop,
    isAddress,
    springs,
  }
}
