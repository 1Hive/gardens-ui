{
  "name": "connect-honey-pot-example",
  "main": "dist/index.js",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "build": "yarn clean && yarn compile",
    "dev": "yarn build --watch",
    "clean": "rm -rf ./dist ./tsconfig.tsbuildinfo",
    "compile": "tsc",
    "start": "ts-node ."
  },
  "dependencies": {
    "@1hive/connect-honey-pot": "link:..",
    "@aragon/connect": "0.7.0",
    "@aragon/connect-core": "0.7.0",
    "@aragon/connect-thegraph": "0.7.0"
  },
  "devDependencies": {
    "ts-node": "^8.10.2",
    "typescript": "^3.8.3"
  }
}
